/**
 * Created by Bat-Orgil on 7/13/2015.
 */
public class DP {

    /*

        myfunc(n,p)
        r[0...n] // new array with -inf     // Keeps track of the answer.
        pi[0...n] // new array, keeps track of the optimal figure for the value obtained in r

        profit= dp(n,p,r,pi)

        ans={}
        x=price[n];
        while x>0
            ans= ans+pi(x)
            x=x-pix]
        return profit,ans

    */
}
